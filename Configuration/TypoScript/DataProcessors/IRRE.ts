// if.isTrue.field = ce_accordion_item
// table = tx_frontend_domain_model_accordions

pidInList.field = pid

where = parentid=###uid### AND deleted=0 AND hidden=0

orderBy = sorting
markers.uid.field = uid

// as = accordions
